spring.application.name=Sunlingua-Backend

# PostgreSQL Datasource configuration using Railway's environment variables
spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate & JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging settings
logging.level.root=INFO
logging.level.com.sunlingua.sunlinguabackend=DEBUG
logging.file.name=logs/app.log
logging.level.org.springframework.security=DEBUG

# Message and encoding settings
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.main.allow-bean-definition-overriding=true

# JWT and app-specific settings
app.secret-key=123E635266556A586E3272357538786F413F7428472B46250645367566B5970
app.expiration-time=86400000
app.refresh-token.expiration=604800000

# Mailing settings
app.mailing.frontend.activation-url=http://localhost:4200/activate_account
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=linguasin01@gmail.com
spring.mail.password=zyct kcxd zsdr eomo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=noreply@sunlingua.com
